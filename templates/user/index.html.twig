{% extends "base.html.twig" %}

{% block body %}
    <h1>Users</h1>
    <ul>
        {% for user in users %}
            <li>{{user.username}} - {{user.email}}</li>
        {% endfor %}
    </ul>
{% endblock %}